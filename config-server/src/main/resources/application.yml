server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/maulikk04/food-delivery-config-repo.git
          clone-on-start: true
          default-label: main